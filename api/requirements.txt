# Core spine — storage + API dependencies
# Install into the project .venv:  pip install -r api/requirements.txt
sqlalchemy>=2.0.36
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
python-multipart>=0.0.20
